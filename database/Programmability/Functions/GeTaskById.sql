USE WebTaskManagerDb
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE FUNCTION GetTaskById
(	
	@TaskId NVARCHAR(128)
)
RETURNS TABLE 
AS
RETURN 
(
	SELECT TOP 1 * FROM Tasks AS T WHERE T.TaskId = @TaskId
)
GO
